{
  "name": "matrix-ai-proxy",
  "version": "0.1.0",
  "description": "AI-Proxy fuer OpenAI / Gemini (CORS-Workaround). Anthropic geht direkt aus dem Browser via dangerous-direct-browser-access-Flag.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
