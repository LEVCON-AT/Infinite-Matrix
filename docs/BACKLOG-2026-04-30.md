# Offene Punkte — Gesamt-Backlog

**Stand:** 2026-05-13. **Letzter relevanter Commit:** `07b1080` (F.4 Default-Invite-Role).

> **Update 2026-05-13 (Refresh):** Backlog-State-Review nach laengerer Welle-WV/§13.3-V2-Polish + Pre-V1-Defekt-Sweep. Status-Anpassungen unten: A-Bucket Defekte erledigt (B1-D-011 + B1-A-006-Restposten), Welle-B implementation komplett LIVE (Backlog war stale: B.0/B.1/B.2/B.3 stehen), Welle-A Folgesprints A.5+A.6 LIVE (WelcomeModal + CellSuggestModal), F-Bundle 5/6 erledigt (F.1+F.2+F.4+F.5+F.6, nur F.3 Logo/Icon defer wegen Storage-Bucket).
>
> **Update 2026-05-04:** Konzept-Sprint Widget+Vorlagen-Foundation gestartet. Konzept-File `docs/concepts/widget-vorlagen-foundation.md` + Review-Worksheet `widget-vorlagen-review.md/.csv`. Foundation-Direktive „Integration-First, Native-Fallback" als verbindliches Manifest in `architektur.md` §14 verankert + Memory `feedback_native_as_fallback.md`. T.2 (Layer 4) verschoben hinter Konzept-Approval, Naming auf `atom_comments` korrigiert. Sidebar-V1 (Abschnitt I) bleibt parkiert bis User-Spec. Drei neue Vision-V2+-Items unten ergaenzt.

Diese Datei konsolidiert alle offenen Punkte aus den Plan-Dokumenten
(`docs/plan-*.md`), den Memory-Files mit aktivem Backlog-Anteil, der
AU-B1-Audit-Defer-Liste und dem alten `BACKLOG.md` (Standalone-Phase-0).

**Quellenpfade:**
- `docs/plan-backend-phase-0.md` (✅ Phase 0 + Audit-Welle abgeschlossen, 8/9 Erfolgskriterien grün, #8 Backup deferred)
- `docs/plan-phase-1.md` (✅ Sub-Sprints P1.0–P1.D LIVE)
- `docs/plan-bridge.md` (✅ Phase 4 + V2.2 deployed, Phase 5+ deferred als SaaS-Roadmap)
- `docs/plan-user-backend.md` (Welle A teilweise live, B/C/D/E/F offen)
- `docs/audit/B1-summary-2026-04-29.md` (164 Findings, 15+1 CRITICAL + 44 HIGH gefixt; Defer-Liste offen)
- Memory-Files: `project_phase1_state.md`, `project_object_layer_phase3.md`, `project_backend_phase0_state.md`, `project_nodetree_avatar_risks.md`
- `BACKLOG.md` (Standalone-/SaaS-Roadmap-Restposten)

---

## A) Sofort blockierend / Pre-V1-Pflicht

Aus AU-B1-Audit (verbleibend nach den 17 Fix-Wellen K1–K11e):

| ID | Severity | Effort | Status | Titel |
|---|---|---|---|---|
| B1-G-006 | HIGH | M | DEFER | Off-VPS-Backup-Strategie (Hetzner Storage Box + restic, Phase-1.5 — Infra-Aufgabe, kein Code) |
| B1-D-011 | MEDIUM | M | ✅ LIVE 2026-05-13 (`e450884`) | NewCellWizard doppelter `<Show>`-Block — Refactor zu single-callback-form |
| B1-A-006-Restposten | HIGH | M | ✅ LIVE 2026-05-13 (`d4023ac` + `ec5c9ec`) | Subtree-IMPORT-Remap für Object-Layer (Cell + Matrix + Board Pfade) |
| Bridge-Tools `objects.ts/groups.ts` | MEDIUM | L | DEFER | Standalone-Bridge-Side (heute nur Frontend-AI-Pipe-Tools live) — wenn Bridge je auf SaaS umgebaut wird |

---

## B) Welle B — Auth/SSO/MFA + Admin-Dashboard

**Status 2026-05-13 (Refresh):** Implementation grossteils LIVE — Backlog war stale. Bestaetigt ueber Code-Review (Login.tsx mit SSO-Buttons, components/admin/-Sections, auth-mfa-gate.ts, auth-step-up.ts, StepUpDialog.tsx).

| Sub-Sprint | Status | Inhalt |
|---|---|---|
| **B.0** | ✅ LIVE | Admin-Dashboard Foundation: `components/admin/{Platform-Admins,System-Config,Audit-Log,Stats,ProviderSlots}Section.tsx` + `lib/admin.ts` (list_system_config/get/set RPCs). |
| **B.1** | ✅ LIVE | SSO Google + Microsoft + Email+Password + Magic-Link: Login.tsx mit Auto-Gate (signInWithGoogle/Microsoft), Signup.tsx, ResetPassword.tsx. |
| **B.2** | ✅ LIVE | TOTP-MFA + Gate: `lib/auth-mfa-gate.ts` (factors-check, signOut bei Cancel) + `MfaGateDialog`. |
| **B.3** | ✅ LIVE | Step-Up-Auth AAL2: `lib/auth-step-up.ts` (requireFreshAal2, 5min-Frische) + `StepUpDialog`. Genutzt in deleteWorkspace + transferWorkspaceOwnership. |
| **B.4** | TODO | Audit-Coverage Auth-Events (password_changed, mfa_enrolled, session_revoked) — moegliche Erweiterung von `system_audit_log` (Migration 039). |
| **B.5** | TODO | Session-Mgmt + Device-Liste (`list_my_sessions` RPC, Revoke selektiv + alle anderen). |

---

## C) Welle A — KI-First Folgesprints (A.0/A.1/A.2/A.3/A.4 LIVE)

| ID | Status | Inhalt |
|---|---|---|
| **A.5** | ✅ LIVE V1 | Eingeladener-User-Tour (`WelcomeModal.tsx`, 3-Slide-Tour mit Skip-Button, `welcome-tour.ts` Persistence). V2 Polish (real Page-Tooltips statt Modal-Tour) defer. |
| **A.6** | ✅ LIVE V1 (`5d50278` parentLabel-Fix) | Cell-Suggest-Modal: `CellSuggestModal.tsx` + `lib/cell-suggest.ts` Singleton-State + `runAssist(mode='cell-suggest')`. Trigger via „✨ AI vorschlagen"-Button im NewCellWizard. parentLabel jetzt „Zeile × Spalte"-Form. |

---

## D) Welle D — User-Profile & Account-Lifecycle (noch leer)

| # | Aufwand | Inhalt |
|---|---|---|
| **D.1** | 0.5 Sprint | Display-Name + Email-Änderung (mit Verify-Roundtrip) |
| **D.2** | 1 Sprint | Avatar-Upload (Storage-Bucket + AccountProfile + Render-Stellen) |
| **D.3** | 1 Sprint | Bio + Timezone + Language (`user_profiles`-Tabelle); inkl. Recur-TZ-Aware-Refactor |
| **D.4** | 1 Sprint | Account-Löschung Self-Service (`delete_self_account` RPC; braucht B.3) |
| **D.5** | 1 Sprint | Multi-Session-Management (überschneidet sich mit Welle B.5 — nur einer braucht's) |

---

## E) Welle E — Wiki/Doku-Sektion (noch leer)

| # | Aufwand | Inhalt |
|---|---|---|
| **E.1** | 0.5 Sprint | `wiki_pages`-Schema (Workspace + Plattform-Doku, Hierarchie 3-4 Ebenen) |
| **E.2** | 2 Sprints | `/w/:id/wiki`-Tab + Tree-View + Markdown-Editor + Volltextsuche |
| **E.3** | 0.5 Sprint | Tool-Help-Section mit Plattform-Pages (`/help/getting-started`) |
| **E.4** | 1 Sprint | API-Doku-Generation aus MCP-Tool-Schema (braucht A.1) |
| **E.5** | Defer | Wiki-Versionen + Attachments (Phase 3+) |

---

## F) Welle F — Workspace-Polish (Bundle-Sprint)

**Status 2026-05-13:** 5/6 erledigt; F.3 hängt an Storage-Bucket-Foundation.

| # | Aufwand | Status | Inhalt |
|---|---|---|---|
| **F.1** | 0.2 Sprint | ✅ LIVE (`558c0d2`) | Workspace-Rename — Inline-Edit in `WorkspaceGeneral.tsx`, RLS-gestuetzt fuer owner+admin. |
| **F.2** | 0.3 Sprint | ✅ LIVE (`1d1150c` + Migration 083) | Workspace-Description (max 500 chars, optional). |
| **F.3** | 0.3 Sprint | TODO | Workspace-Logo/Icon (Storage-Bucket-Pattern aus D.2). Erst nach D.2 Avatar-Foundation. |
| **F.4** | 0.2 Sprint | ✅ LIVE (`07b1080` + Migration 084) | Default-Rolle pro Workspace (`default_invite_role` enum editor/viewer, Settings-Dropdown). |
| **F.5** | 0.2 Sprint | ✅ LIVE (`18f0122`) | Email-Verification-Status-Badge im AccountProfile (teal=verified / amber=pending). |
| **F.6** | 0.2 Sprint | ✅ LIVE (`18f0122`) | Resend-Verification-Mail-Button (`supabase.auth.resend({type:'signup',email})`). |

**Bundle-Status:** 0.2 Sprints uebrig (F.3 nach D.2-Storage-Bucket).

---

## G) Welle C — Integrations / Webhooks (noch leer, Phase 2)

| # | Inhalt |
|---|---|
| **C.1** | `workspace_events` + `workspace_webhooks` + dispatch-Service mit HMAC + SSRF-Schutz |
| **C.2** | In-App-Inbox: `notifications`-Tabelle + Glocken-Icon + Read-State |
| **C.3** | Quick-Rules: 3-4 Templates (Checklist closed → Webhook etc.) |

---

## H) Welle O — Object-Layer Folgesprints (O.1-O.8.N LIVE)

| ID | Inhalt |
|---|---|
| **O.9** | Type-Schema mit validierten Attributen (z.B. „Person" hat email/phone als typed Fields) |
| **O.10** | Volltext-Suche für Object-Mentions in Doku/Checklist-Texten |

---

## H2) Welle T — Task-Layer (Phase 4, T.1 + T.AC LIVE)

T.1.A bis T.1.K + T.AC.A-D live (Stand 2026-05-01). ECS-Architektur: `tasks` (Layer 0) + `task_manifestations` (Layer 1) + polymorphe `atom_manifestations` fuer Link/Checklist. Eine Wahrheit, viele Linsen. Drag-to-Create-Manifestation als USP. Recur-Expansion im Calendar mit done-pro-Instanz.

| ID | Status | Inhalt |
|---|---|---|
| **T.1.A-K** | ✅ live | Schema + Migration + Mutations + UI-Adapter + Smart-Summary + Agenda + Calendar + Drag-Drop + MCP-Tools + Export |
| **T.1.D5/6/7** | DEFERRED | UI-Refactor BoardView/ChecklistPanel auf native KanbanCard/ChecklistItemView-Types (heute via Projection-Helper). UX gleich, Aufwand gross. |
| **T.AC.A-C + D.1-3** | ✅ live | Schema atom_manifestations + Sync-Trigger + Realtime + Drag-Sources Link/Checklist + atom-aware Click + Inline-Remove + Recur+Range im Modal + Recur-Expansion + done-pro-Instanz. |
| **T.AC.A.4** | DEFERRED | task_manifestations dropt (113-File-Refactor, kein User-Wert; Sync-Trigger reicht). |
| **T.AC.D.4** | DEFERRED | Recur-Edit nach Drop (heute: ✕ + neu). |
| **T.SS** | TODO (verschoben) | Smart-Summary-Dashboard — wird zur Plattform-Default-Vorlage „Smart Summary" im Widget-Vorlagen-Modell (siehe `docs/concepts/widget-vorlagen-foundation.md` §11). Erst nach Konzept-Approval. |
| **T.2** | TODO (verschoben) | Layer-4 Additive Anhaenge — **`atom_comments`** (polymorph, korrigiert von `task_comments`) + `atom_attachments`. Pro Foundation-Direktive (§14): primaer **Channel-Bridge** zu Mail/Messenger/Slack/Teams, native nur als opt-in Fallback. Erst nach Widget-Vorlagen-Welle A. |
| **T.3** | TODO | Layer-2 Dependencies — `task_dependencies (from_task_id, to_task_id, kind)`, Cycle-Detection, Visual-DAG-Renderer. |
| **T.4** | TODO | Layer-3 Rules/Trigger — `task_rules (when_jsonb, then_jsonb)`, Server-Side via Postgres-Trigger oder Edge-Function. Heikelster Block, evtl. erst V2. |
| **T.1.G.2.D** | TODO | NodeTree-Cell als Drop-Target (Container-Disambiguation Board vs Checklist). Folgesprint von T.1.G.2.C. |

---

## I) Sidebar-V1-Wünsche (User-Vorbehalt)

User hat angekündigt: „die finalen Änderungen der V1 (da gibt es noch ein paar Dinge, vor allem in der Sidebar)". Spec liegt noch nicht vor — parkiert bis User-Spec, **danach** Widget+Vorlagen-Welle. NodeTree-Avatar-Risiken R1-R5 (`memory/project_nodetree_avatar_risks.md`) als Wiedervorlage.

---

## I-WV) Widget+Vorlagen-Foundation (Konzept-Sprint 2026-05-04, GROESSTER UMBAU vor V1-Abschluss)

User-Direktive 2026-05-04: alle Features ausser Matrix sind im Kern Vorlagen mit Symbol/Hotkey/Widgets. Konzept-File `docs/concepts/widget-vorlagen-foundation.md`, Review-Worksheet `widget-vorlagen-review.md/.csv`.

| Welle | Inhalt | Aufwand |
|---|---|---|
| **WV.A** | Foundation: feature_templates + cell_template_instances + Plattform-Defaults + Widget-Renderer | ~15d |
| **WV.B** | Atom-Erweiterung: `info_field`-Atom (10 typed Field-Types) + `link.provider`-Diskriminator (15 Werte) + **`links.click_count int default 0`** mit Increment-Trigger (§11.7) + **`info_fields.symbol_override` + `links.symbol_override`** (§12.3.5 User-Override-Persistenz) + **Symbol-System** mit Auto-Mapping pro Field-Type/Provider + Favicon-Fetch (Service-Worker-Cache, TTL 30d) + Brand-SVG-Assets in `client-web/src/assets/brand-icons/` + Lazy-Render via IntersectionObserver + Backfill `cell.data.infoFields/links` (Clean-cut) | ~10d |
| **WV.C** | Vorlagen-CRUD + Bulk-Apply: UI Save-as-Template + Multi-Select + Bulk-Hotkey | ~12d |
| **WV.D** | Channel-Bridges: OneNote (Doc-Sync), Outlook-Mail, OneDrive (Attachments), Public-Alias-Resolve, AI-Tool `alias.expand_to_text` | ~25d |
| **WV.E** | Cross-View-Komplettierung: Doc-Cross-View (Drop-Targets), Auto-Calendar aus typed Date-Field (§9.14), **Calendar-Outbound-Sync** (§10.5 final 2026-05-07: Google + Outlook/Graph + Microsoft365, bidirektional mit External-Last-Write-Wins-Konfliktaufloesung, OAuth-Re-Auth mit Calendar-Write-Scope beim Aktivieren) | ~10d |
| **WV.F** | Polish: Activity-Multi-Source, Marker-Foundation, Designer-First-Route (V2-Frage) | ~10d |
| **WV.X — Picker-Audit-Sprint** (deferred, Timing TBD — User 2026-05-07: „muss nicht jetzt sein") | Refactor der heutigen Picker-Foundation. Pflicht-Output: **Matrix mit Ist / Soll / Vereinheitlichung pro existing Picker-Fall im Code** — Alias (`use-alias-autocomplete`, `alias-index`, `alias-chip-menu`), Object (`ObjectPickerModal`, `ObjectSuggestion`, `use-object-suggest`), Tag (`tag-index`, `atom-tags` — kein Picker existing), User (`pm-mention-plugin` — nur PM, kein generischer Picker), Atom (`AtomPickerModal`), Cell (`CellSuggestModal`) + Audit-Entdeckungen. Detail-Entscheidungen (Render-Linsen-Modell, PM-Mention-Scope, Object-Doppel-Pfad, Tag/User-Generizitaet) leiten aus Matrix ab. Welle WV.A nutzt heutige Picker as-is, der Audit-Sprint zieht spaeter ein. Siehe `docs/concepts/widget-vorlagen-foundation.md` §5.2.3 + §17 R-WV-12. | TBD |
| **WV.Y — Atom-Filter-Attribute-Audit** (Pre-WV.A-Pflicht, deferred — User 2026-05-07: „Variante B und Drops zum gegeben Zeitpunkt besprechen") | Pflicht-Output pro Atom-Type (`task`, `doc`, `link`, `checklist`, `info_field`, `imported_event`) pro Attribut: **Spalten-Mapping** (existing / needs migration / drop) + **UI-Form** (Select / Date-Range / Numeric / Boolean / Free-Text / Multi-Select) + **Index-Pflicht** (BTREE / GIN / Partial / none) + **Translation-String** + **Drop-Diskussion** (Kandidaten wie `has_subtasks`, `domain` pruefen). Output: `lib/atom-filter-attrs.ts` als TS-Konstante (Single-Source fuer Filter-Builder + Saved-Filters + AI-Tool `query.run` + Sidebar-Filter + Command-Palette). Liste in Konzept §17.1 bleibt V1-Vorschlag bis Audit-Final. Detail-Entscheidungen (Scope kuenftige Atome, JSON vs TS-Form, User-Custom-Attribute) leiten aus Audit ab. Siehe §17.1 + §17 R-WV-10. | TBD |
| **WV.Z — Fundus-Foundation** (Konzept-Sub-Sprint, deferred — User 2026-05-07 zu §8.2: Atome statt destruktiv loeschen in einen „Fundus" verschieben mit Restore-Faehigkeit) | **Eigene Matrix auf Workspace-Root** (oder abgelegener Container), visuell unterscheidbar zur aktiven Arbeitsmatrix. Pflicht-Output: Detail-Diskussion F.1-F.12 (Form · Atom-Status · Manifestationen · Alias-Resolution · Schema · Berechtigungen · Restore-Pfad · Beziehung zu Export-Doppelboden · Sub-Strukturen · Auto-Cleanup · Migration heutiger Soft-Delete-Pfade · Foundation-§14-Probe). Schema-Heptad fuer `fundus_*`-Tabellen. UI-Spec fuer Fundus-Container + Restore-UX. Verankert im Confirm-Modal §8.2 (zwei Knoepfe „Loeschen mit Auswirkungen" / „In Fundus verschieben") sobald implementiert. Bis dahin nur Hard-Delete + Export-Checkbox als Doppelboden. Siehe `docs/concepts/widget-vorlagen-foundation.md` §14a. | TBD |
| **WV.WV — Atom-Verankerungs-Konsolidierung + Generic-Drop-Refactor + atom_tags-Drift-Audit + Drag-Hover-Navigator + WidgetPicker + Card-Polymorphie + AdapterDialog** (kombinierter Pre-WV.A-Pflicht-Sprint, deferred — User 2026-05-07 zu §9: „alles in Richtung gold-generic-standard") | **Acht Outputs in einem Sprint:** **(1) atom_pins-Konsolidierung** in `atom_manifestations(kind='pinned')` mit polymorphem `container_id` + neue Discriminator-Spalte `container_kind` ∈ {kanban-col, checklist, calendar, cell, atom, node}. Clean-Cut-Migration (Memory `feedback_clean_cut_no_prod_data.md` — staging-Bestand ~2 Pins). atom_pins-Tabelle wird gedroppt, Cascade-Trigger re-kodiert in atom_manifestations. **(2) Generic-Drop-Refactor:** `manifestation-cross-view.ts` Helper akzeptieren `(atomType, atomId, ...)` — neue Funktionen `dropAtomOnKanbanCol`, `dropAtomOnChecklist`, `dropAtomOnCell`, `dropAtomOnAtom`, `dropAtomOnNode`. Drop-Targets in `BoardView`, `ChecklistPanel`, `SidebarCalendarMini`, `SidebarDayView`, **Hauptkalender-Route `Calendar.tsx`** `accepts:`-Filter aufweichen — alle **5** Atom-Typen (inkl. imported_event-Drag-Source-Freischaltung §9.12). DragAtomType erweitert um `'imported_event'` als 5. draggable Atom-Type. Calendar-zu-Calendar-Drop = Wiedervorlage-Manifestation. Card<imported_event>-Edit-Mode-Schutz: kein ✏️, nur X. Card-Komponente in BoardView wird polymorph (`Card<AtomType>`), ChecklistPanel-Item analog. **(3) atom_tags-Drift-Audit:** pruefen ob heute Code atom_tags strukturell missbraucht (Render statt Filter) — Drift-Befund → re-mappen auf atom_manifestations(kind='pinned'). atom_tags BLEIBT erhalten (alle 4 kinds — fachliche Bewertung 2026-05-07). **(4) Drag-Hover-Navigator-Komponente** (User 2026-05-07 zu §9.6c, querschnittlich §9.9/§9.10/§9.13): NavTree expandiert auf Drag-Hover als Overlay mit Backdrop-Blur + Puls-Animation, Drag-State persistent, User navigiert zur Ziel-Cell, Drop landet in entferntem Widget. Plus Modal-Variante (Doc-Editor → NavTree-Suche). Anlage gemaess `code-quality.md` §6.5, `style.md` §6.5-Pattern eingetragen. **(5) WidgetPicker-Komponente** (User 2026-05-07 zu §9.9 + §9.10 — generisch, ueberholt urspruengliche KanbanColPicker-Wahl): Zeigt alle kompatiblen Widget-Slots in einer Cell nach Atom-Drop/Paste — **Root-Widgets prominent gelistet** (oben), non-Root als Fallback. **Multi-Root-Disambiguierung** (mehrere Vorlagen koennen jeweils das gleiche Atom-Type als Root-Widget haben). Routing-Logik: 0 kompatibel=Confirm Neue-Vorlage / 1=direkt / ≥2 mit Multi-Root=Picker prominent / ≥2 mit 1-Root=direkt im Root / ≥2 ohne Root=Picker. Modifier-Key `Shift+Drop` erzwingt Picker auch bei Auto-Routing. Einheitlich Desktop + Mobile. Reuse-Faelle: doc/link/checklist-Drop in beliebigem Widget-Slot, Bulk-Apply auf Cells mit mehrdeutigem Ziel, Form-Field-Slot-Wahl. Plus **`feature_templates.root_widget_id`-Schema-Erweiterung** (§9.10a) — neue Spalte als Default-Drop-Target-Pointer mit Vorlage-Designer-Toggle. `style.md`-Pattern „WidgetPicker" bei Implementierung. **(6) Card<AtomType>-Polymorphie:** BoardView-Card-Komponente bekommt Atom-Type-spezifische Render-Varianten — `Card<task>` (existing, bleibt), `Card<link>` (Title + Domain + click_count), `Card<doc>` (Title + Mini-Excerpt 80 Zeichen + Doc-Icon + Pin-Count-Badge), `Card<checklist>` (Title + Item-Count + Progress-Bar; Hybrid mit Click-Expand-Toggle gemaess §9.7), `Card<info_field>` (post-WV.B mit Title + Value-typed-rendered + Origin-Badge; Click drillt zur Source-Cell mit Feld-Focus + Pulse), `Card<imported_event>` (Title + Provider-Color + Date-Subtitle + URL-Open-Button; kein ✏️, nur X). Plus 9.8a-Erweiterung im `CreateManifestationModal`/Detail-Modal: Items-Preview fuer Checklist-Calendar-Termine (erste 3-5 Items + Checkbox-Status + Open-Button) sowie Doc-Excerpt-Preview fuer Doc-Calendar-Termine. **(7) AdapterDialog-Komponente** (User 2026-05-07 zu §9.13.7 als Querschnitt §9.C): generischer Mapping-Dialog wenn Atom-Type-A auf Widget fuer Atom-Type-B gedroppt wird. Default-Mapping aus Atom-A-Feldern + User-Override + Submit erzeugt Atom-B oder Pseudo-Atom mit Snapshot. Auto-Adapter ohne Dialog wenn 1:1-Mapping eindeutig. Reuse-Faelle: info_field(url)→Link (Anzeigetext+URL-Dialog), info_field(date)→Calendar (auto), doc→Kanban-Card (auto), link→Doc-Pin (auto), info_field(text)→Doc-Pin (Embed-vs-Reference-Dialog). Anlage gemaess `code-quality.md` §6.5. **(8) Custom-MIME-Rename + JSON-Format** (User 2026-05-07 zu §14.5): `application/x-matrix-atom` → `application/x-matrix-atom-ref` (eindeutiger als „Atom-Ref" konsistent zu Architektur-Manifest §1.6 `AtomRef`-Type). Format-Erweiterung von String `${atomType}:${atomId}` zu JSON-Payload `JSON.stringify({ atomType, atomId, workspaceId })` — Cross-Workspace-Drop-faehig + erweiterbar (kuenftige Felder `aliasHint`, `displayMeta`-Snapshot). Migration trivial: 1 Set-Stelle (`drag-context.ts:84`) + alle Get-Handler (Audit beim Sprint via Grep `application/x-matrix-atom`). Kein Bestand-User-Konflikt — Drag-Sessions persistieren nicht ueber Releases. **Code-Auswirkung:** `lib/atom-pins.ts`, `components/AtomPickerModal.tsx`, `components/DocsPopup.tsx`, `lib/docs-open.ts`, `lib/docs-ui.ts`, `lib/subtree-import.ts`, MCP-Tools `bridge/src/tools/atom-pin.ts`, Export/Import (D.X.E), neuer `components/DragHoverNavigator.tsx`, neuer `components/WidgetPicker.tsx`, `components/BoardView.tsx` Card-Komponente, `feature_templates.root_widget_id`-Migration, Vorlage-Designer-Inspector mit Root-Widget-Toggle, `components/DocsPopup.tsx`-Toolbar „Als Checkliste anlegen" + `/checklist`-Slash-Command, `lib/drag-context.ts:84` (MIME-Rename + JSON-Payload) + alle dataTransfer.getData-Handler (Audit) — alle re-routen oder neu. Welle-D-Funktionalitaet bleibt erhalten. Siehe `docs/concepts/widget-vorlagen-foundation.md` §9 + §9.A + §9.B + §14.5 + Memory `project_welle_d_doku_state.md` + `feedback_drag_hover_navigation.md`. | TBD |
| **Total** | | ~80-120d / 15-20 Sprints (+ WV.X + WV.Y + WV.Z + WV.WV TBD) |

Roadmap-Position laut User: dieser Umbau **vor** End-Restaufraeumen, BACKLOG-Final-Pass, SaaS-Page, Produktdoku, Test/Prod-Infra V2.

---

## V) Vision V2+ (neu 2026-05-04)

Items, die nach dem Widget+Vorlagen-Refactor kommen:

| ID | Inhalt |
|---|---|
| **V2.1 Teilen-via-Drag-Drop nach extern** | Inhalte/Boards via Mail / Messenger / Chat-KI teilen, Drag-Drop-Quelle = Atom oder Widget, Ziel = externes App-Window oder Compose-Field. Alias-Aufloesung zu absoluter URL. Mental-Modell User-KI: *„schreib bitte an xy@ alle Informationen aus ^xy und fuehr alle Hyperlinks mit Dokumenten unten an"*. Setzt WV.D voraus (Public-Alias-Resolve + AI-Tool). |
| **V2.2 AI-Bucket-Dashboard-Generierung** | KI erstellt on-demand Dashboard aus Vorlage + Filter. *„KI mach mir bitte ein Bucket aller Zellen mit Vorlage ‚Info Vertrag' mit den wichtigsten Infos"*. Setzt WV.A-C voraus (Vorlagen-Modell + Bulk-Apply als Bauteil). |
| **V2.3 Externe Channel-Sammelwelle** | Folge-Provider zu WV.D: WhatsApp-Business, Telegram, Discord (Comments), Box, iCloud-Drive (Attachments), Notion-DB (Info-Sync), Trello/Jira/Asana (Kanban-Sync), Todoist/MS-To-Do (Checklist-Sync). |

---

## J) Phase-1.5 (deferred Items aus Phase 1)

| ID | Inhalt |
|---|---|
| Off-VPS-Backup | identisch B1-G-006 (Hetzner + age + restic + DR-Drill) |
| **P1.0b** | Service-Role-Key-Rotation (rotate-Skript + Bridge atomarer Re-Load + Bitwarden-Workflow, ~45 min) |
| supabase_admin-PW-Sync | Idempotenter Admin-Password-Sync-Step (verhindert das PW-Drift, das uns 2026-04-26 in CI gestoppt hat) |
| JWT-Rotation | Service-Key-Rotation analog Bridge |
| Globale sshd-Härtung | `PasswordAuthentication no` global + `PermitRootLogin prohibit-password` |

---

## K) NodeTree-Avatar-Risiken (Wiedervorlage, noch nicht eingetreten)

5 Risiken mit Fallbacks dokumentiert in `project_nodetree_avatar_risks.md`. Pflicht-Check vor Production-Workload:

| ID | Trigger / Auswirkung | Fallback |
|---|---|---|
| **R1** | Presence-Filter-Last bei >100 Tree-Rows + >5 aktive User | Filter ein-mal in NodeTree-Wurzel als `Map<entryId, PresenceUser[]>` aufbauen |
| **R2** | Migration-Backfill blockiert >10k Zeilen | Chunk-UPDATE in Schleife |
| **R3** | `auth.uid()`-Default greift nicht bei Service-Role-Inserts | Bridge-Tools mit explicit-Param `created_by` |
| **R4** | Avatar-Click triggert Doppel-Navigation | `pointerdown` + `stopImmediatePropagation` |
| **R5** | Hash-Scroll feuert nicht bei Same-Route-Hash-Wechsel | `requestAnimationFrame`-Scroll-Aufruf nachstellen |

---

## L) Quer-Themen aus plan-user-backend.md

| ID | Inhalt |
|---|---|
| Timezone in Recurring-Cards | UTC-only heute → Wien-User sieht Daily 09:00 als UTC. Braucht D.3 als Voraussetzung. |
| API-Schema-Export / OpenAPI | Auto-Generation aus MCP-Tool-Schema (Anker für E.4) |
| GitHub-Secret `STAGING_SUPABASE_ANON_KEY` | User-Action — Wert aus VPS `.env` ins GitHub-Settings übertragen, sonst CI-Build broken |

---

## M) AU-B1-Audit-Defer-Liste (MEDIUMs/LOWs gesammelt)

55 MEDIUM + 26 LOW Findings, konsolidiert nach Theme:

| Theme | Effort | Items |
|---|---|---|
| **UI-Tokens-Polish** | XS-S, ~10 Findings | Hex-Literals → Tokens (B1-D-013/014, B1-E-008/014/018/019, B1-G-018/020), GroupMatrixGenerator Mobile-Breakpoint, Wizard-Buttons `.btn-p`, ARIA-Labels Wizard-Step-Dots, aria-live für Streaming-Text |
| **Performance-Polish** | S-M, ~6 Findings | resolverMaps Memo-Granularität, `boardIds`-equals-Guard, `putAll()` Promise.all, createMemo statt const-shadowing |
| **Doku-Refresh** | S, ~4 Findings | Plan-Dokumente synchronisieren mit Memory-Wahrheit |
| **Bridge-Coverage** | M-L, ~3 Findings | `doc.*`-Tools, `query.aliases doc`-Kind, Object-Layer-Tools für Bridge-Side |
| **Settings-Search** | S, 1 Finding | Object-Routen aus Index oder dokumentieren |
| **Misc** | XS-S, ~5 Findings | nginx Staging-vs-Prod-Inkonsistenzen, Kong 2.8 EOL, backup-cron-User, sshd-Auth-Doku |

Volle Liste in `docs/audit/B1-summary-2026-04-29.md` Abschnitt „Defer-V1-Liste".

---

## N) Phase-0-Restposten aus altem `BACKLOG.md`

Die App-Phase 0-Welle ist abgeschlossen, aber 5 Items aus alten Code-Review-Sprints sind explizit als „später wenn Use-Case" markiert:

| Item | Aufwand | Trigger |
|---|---|---|
| **3** Mobile-Gesten | 2 Tage | nur wenn Mobile-Nutzung real wird |
| **4** Visual Templates (Matrix-Vorlagen) | 3 Tage | Produkt-Erweiterung |
| **5** Block-Placing (DnD zwischen Strukturen) | 3-4 Tage | UX-Wunsch |
| **6** Workspace- & Feature-Enrichment (Timeline, Tabelle, Embed-Gallery, Reference, Formula, Today-View, Overdue, Recently-Modified) | 1-2 Wochen | Feature-Roadmap |
| **7/8** Yjs-CRDT + SaaS-Roadmap | mehrere Wochen | Phase 5+ |
| **9** Undo-/Redo komplett (Redo-Stack + Ctrl+Z/Y + erweiterte Wire-Liste) | 1-2 Tage | Polish |

---

## O) HTML-Standalone-Restposten (matrix.html eingefroren, low-prio)

Aus Memory `project_backend_phase0_state.md`:

| Item | Status |
|---|---|
| Minimap | low prio |
| IMX-Crypto-Export | dokumentiert, nicht im SaaS-Pfad |
| Workspace-weiter Export/Import-Entry-Point via Command-Verb | aktuell nur per-Ebene |

---

## Vorgeschlagene Reihenfolge (aus plan-user-backend.md, leicht aktualisiert)

```
Welle 1 (gerade fertig):        AU-B1-Audit + Fix-Welle (15 CRITICAL + 44 HIGH gefixt)
Welle 2 (Sidebar-V1):           User-Wünsche, noch nicht spezifiziert
Welle 3 (Auth):                 B.1 + B.2 + B.3 (~3 Sprints)
Welle 4 (Account):              D.1 + D.5 + F.5/F.6 (~1 Sprint)
Welle 5 (KI-User-facing):       A.5 + A.6 (~1 Sprint)
Welle 6 (Profile-Polish):       D.2 + D.3 + D.4 (~3 Sprints, braucht B.3)
Welle 7 (Workspace-Polish):     F.1-F.4 (~1 Sprint)
Welle 8 (Wiki):                 E.1 + E.2 + E.3 (~3 Sprints)
Welle 9 (Integrations):         C.1 + C.2 + C.3 (~3 Sprints)
Welle 10 (API-Doku):            E.4 (braucht A.1) (~1 Sprint)
Welle 11 (Object-Folge):        O.9 + O.10 (~2-3 Sprints)
Welle 12 (Phase-1.5):           Off-VPS-Backup + Key-Rotation + sshd-Hardening (~1 Sprint)
```

**Grobschätzung total:** ~20-25 Sprints für alles ohne Phase-3-Items (Mail-Inbound, vollständiger Rules-Builder, WebAuthn, voller i18n, Mobile-Gesten, Templates, Block-Placing).

---

## Update-Protokoll

Nach jedem abgeschlossenen Sprint hier:
- Status-Spalte des Items auf ✅ LIVE setzen + Datum + Commit-Hash.
- Bei strukturellen Änderungen am Backlog: Quelle (Memory-File oder Plan-Doku) gleichzeitig synchronisieren.

Memory `feedback_no_pr_direct_merge.md`: Direct-Merge nach `main` pro Sub-Sprint, kein PR.
