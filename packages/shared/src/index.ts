// Shared types and utilities for all packages.
// Will be populated in backend-phase-0:
// - DB row types (from Supabase typegen)
// - Zod schemas shared between bridge and client
// - Common constants (recur types, close modes, etc.)

export const PACKAGE_VERSION = '0.0.1';
