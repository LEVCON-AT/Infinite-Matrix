{
  "name": "@infinite-matrix/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types and utilities for Bridge, Standalone-Client and SaaS-Client.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
